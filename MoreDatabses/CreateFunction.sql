DELIMITER //
DROP FUNCTION IF EXISTS safediv;

CREATE FUNCTION safediv(a INT, b INT)
RETURNS DECIMAL (5, 2)
DETERMINISTIC
BEGIN
	IF b = 0 THEN
		RETURN 0;
	ELSE
		RETURN a / b;
	END IF;
END //
DELIMITER ;
